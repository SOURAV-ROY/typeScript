let a: number;
let b: boolean;
let c: string;
let d: any;
let e: number[] = [1, 2, 3, 4, 5];
let f: any[] = [1, true, 'a', false];

const ColorReed = 3;
const ColorGreen = 4;
const ColorBlue = 5;

enum Color {
    Red,Green,Blue, Purple
}
let backgroudColor = Color.Green;